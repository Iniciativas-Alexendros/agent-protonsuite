# Contract: Profesionalización y estandarización

## Nuevos archivos (a crear)

| Archivo | Tipo | Propósito |
|---------|------|-----------|
| `eslint.config.ts` | Config | ESLint flat config con reglas strict TypeScript |
| `commitlint.config.ts` | Config | Conventional commits con scopes del proyecto |
| `knip.config.ts` | Config | Detección de dependencias/exports muertos |
| `renovate.json5` | Config | Renovate para auto-merge + lockfile maintenance |
| `.releaserc.json` | Config | semantic-release con OIDC npm + GitHub releases |
| `.husky/commit-msg` | Hook | Ejecuta commitlint |
| `.husky/pre-commit` | Hook | Ejecuta lint-staged |
| `.lintstagedrc.json` | Config | Solo ESLint sobre staged files |
| `ROADMAP.md` | Doc | Actualizado con milestones de profesionalización |

## Archivos a modificar

| Archivo | Cambio |
|---------|--------|
| `package.json` | Añadir `lint`, `lint:fix`, `knip`, `prepare` scripts. Añadir devDependencies: `eslint`, `typescript-eslint`, `@eslint/js`, `eslint-plugin-unicorn`, `eslint-plugin-security`, `eslint-plugin-import-x`, `husky`, `@commitlint/cli`, `@commitlint/config-conventional`, `lint-staged`, `knip`, `semantic-release`, `@semantic-release/changelog`, `@semantic-release/github`, `@semantic-release/npm`, `@semantic-release/commit-analyzer`, `@semantic-release/release-notes-generator`, `renovate` (devDep opcional). Versión → `0.7.0` |
| `.github/workflows/ci.yml` | Añadir paso `lint` (ESLint) después de checkout y antes de typecheck |
| `.github/workflows/quality.yml` | Añadir paso `knip` después de SBOM |
| `.github/workflows/release.yml` | Reemplazar lógica manual de version bump + tag + changelog por `npx semantic-release`. Mantener pasos de OIDC, provenance, SBOM, Docker build. |
| `.gitignore` | Añadir `.eslintcache` si se usa caching |
| `CHANGELOG.md` | Reducir a header + nota "Automatically generated by semantic-release". El contenido existente se preserva como histórico. |
| `RELEASE.md` | Actualizar procedimiento: ya no es manual, referenciar semantic-release |
| `CONTRIBUTING.md` | Añadir sección sobre conventional commits forzados por commitlint |

## Contratos de configuración

### eslint.config.ts

```ts
// eslint.config.ts
import tseslint from 'typescript-eslint';
import js from '@eslint/js';
import unicorn from 'eslint-plugin-unicorn';
import security from 'eslint-plugin-security';
import importX from 'eslint-plugin-import-x';

export default tseslint.config(
  { ignores: ['dist/', 'node_modules/', 'coverage/', '.husky/'] },
  js.configs.recommended,
  ...tseslint.configs.strictTypeChecked,
  ...tseslint.configs.stylisticTypeChecked,
  {
    languageOptions: {
      parserOptions: {
        projectService: true,
        tsconfigRootDir: import.meta.dirname,
      },
    },
    plugins: {
      unicorn,
      security,
      'import-x': importX,
    },
    rules: {
      // TypeScript strict
      '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
      '@typescript-eslint/consistent-type-imports': 'error',
      '@typescript-eslint/no-unsafe-assignment': 'off', // too aggressive for this codebase
      '@typescript-eslint/no-unsafe-member-access': 'off',
      '@typescript-eslint/no-unsafe-call': 'off',
      '@typescript-eslint/no-unsafe-return': 'off',
      '@typescript-eslint/restrict-template-expressions': 'off',
      '@typescript-eslint/no-floating-promises': 'error',
      '@typescript-eslint/no-misused-promises': 'error',
      '@typescript-eslint/switch-exhaustiveness-check': 'error',

      // Unicorn (selected)
      'unicorn/prefer-node-protocol': 'error',
      'unicorn/no-array-for-each': 'warn',
      'unicorn/prefer-ternary': 'warn',

      // Security
      'security/detect-object-injection': 'off', // too many false positives

      // Import order
      'import-x/order': ['warn', {
        groups: ['builtin', 'external', 'internal', 'parent', 'sibling'],
        'newlines-between': 'never',
        alphabetize: { order: 'asc' },
      }],
    },
  },
  // Test files: relaxed rules
  {
    files: ['tests/**/*.ts', '**/*.test.ts'],
    rules: {
      '@typescript-eslint/no-floating-promises': 'off',
      '@typescript-eslint/no-misused-promises': 'off',
    },
  },
);
```

### commitlint.config.ts

```ts
export default {
  extends: ['@commitlint/config-conventional'],
  rules: {
    'type-enum': [2, 'always', [
      'feat', 'fix', 'docs', 'style', 'refactor',
      'perf', 'test', 'build', 'ci', 'chore', 'revert'
    ]],
    'scope-enum': [2, 'always', [
      'imap', 'smtp', 'http', 'agent', 'alerts', 'pass',
      'config', 'deps', 'release', 'ci', 'docs', 'tests'
    ]],
  },
};
```

### knip.config.ts

```ts
export default {
  entry: ['src/index.ts', 'src/agent-cli.ts'],
  project: ['src/**/*.ts', 'tests/**/*.ts', 'scripts/**/*.sh'],
  ignoreDependencies: [
    '@types/node',
    '@types/express',
    'eslint',            // used by eslint.config.ts (not in source)
    'typescript-eslint', // used by eslint.config.ts
  ],
  ignore: [
    'src/version.ts',    // dynamically reads package.json
    'eslint.config.ts',
    'commitlint.config.ts',
    'knip.config.ts',
  ],
  rules: {
    files: 'error',
    dependencies: 'error',
    devDependencies: 'warn',
    exports: 'warn',
  },
};
```

### .releaserc.json

```json
{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    ["@semantic-release/changelog", {
      "changelogFile": "CHANGELOG.md",
      "changelogTitle": "# Changelog"
    }],
    ["@semantic-release/npm", {
      "npmPublish": true
    }],
    ["@semantic-release/github", {
      "successComment": false,
      "failComment": false
    }],
    ["@semantic-release/git", {
      "assets": ["CHANGELOG.md", "package.json", "package-lock.json"],
      "message": "chore(release): ${nextRelease.version}\n\n[skip ci]"
    }]
  ]
}
```

### renovate.json5

```json5
{
  $schema: "https://docs.renovatebot.com/renovate-schema.json",
  extends: ["config:recommended"],
  labels: ["dependencies"],
  schedule: ["before 10am on monday"],
  packageRules: [
    {
      matchDepTypes: ["devDependencies"],
      matchUpdateTypes: ["minor", "patch"],
      groupName: "dev dependencies (non-major)",
      automerge: true,
    },
    {
      matchDepTypes: ["dependencies"],
      matchUpdateTypes: ["minor", "patch"],
      groupName: "production dependencies (non-major)",
      automerge: false,
    },
    {
      matchPackageNames: ["@modelcontextprotocol/sdk"],
      groupName: "MCP SDK",
      automerge: false,
    },
  ],
  lockFileMaintenance: {
    enabled: true,
    schedule: ["before 10am on monday"],
  },
  platformAutomerge: true,
}
```

### Scripts a añadir en package.json

```json
{
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "knip": "knip",
    "prepare": "husky"
  }
}
```

### .lintstagedrc.json

```json
{
  "*.ts": ["eslint --fix --no-warn-ignored"]
}
```

## Dependencias nuevas (todas devDependencies)

```
eslint ^9
@eslint/js ^9
typescript-eslint ^8
eslint-plugin-unicorn ^56
eslint-plugin-security ^3
eslint-plugin-import-x ^4
husky ^9
@commitlint/cli ^19
@commitlint/config-conventional ^19
lint-staged ^15
knip ^5
semantic-release ^24
@semantic-release/changelog ^6
@semantic-release/github ^10
@semantic-release/npm ^11
@semantic-release/commit-analyzer ^12
@semantic-release/release-notes-generator ^13
@semantic-release/git ^10
renovate ^39 (optional — runs as GitHub App or self-hosted)
```

## Verificación de contratos

1. `npx eslint .` no produce errores (warnings aceptables)
2. `npx commitlint --from HEAD~1` pasa
3. `npx knip` reporta 0 dependencias sin usar, 0 archivos sin usar
4. `npx semantic-release --dry-run --no-ci` no produce errores
5. `npx renovate-config-validator` pasa (si renovate.json5 existe)
6. `npm run lint` → exit code 0
7. `npm run lint:fix` → modifica archivos y exit code 0
8. `npm run knip` → exit code 0
